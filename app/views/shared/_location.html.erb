<div class="location" id="location_<%= location_counter + 1 %>">  
  <div class="location-detail">
    <%= link_to(location.name, location_url(location)) %> -
    <%= location.people.count %> visits
    <br />
    Currently <%= location.current_visitors.count %> team members present
  </div>
</div>
